{
  "name": "sagewai-adaptive-intelligence-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "smoke": "node --check workflow.js && node --check app.js && node --test workflow.test.js",
    "test": "node --test workflow.test.js",
    "serve": "python3 -m http.server 4174 --bind 127.0.0.1"
  }
}
