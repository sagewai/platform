# Observatory

Cost tracking, OpenTelemetry pipeline, mission-control HUD, Grafana board, audit log. Observatory is the surface that answers your CFO's question — *"why did the AI bill quadruple?"* — with a live, per-tenant breakdown they can read directly.

Two dashboards read the same telemetry stream so they never disagree: the HUD for live operations (agents on a graph, missions in flight, fleet posture across the top) and a Grafana board for analytics, finance, and historical query.

## What you can do with it

- **Track cost per project, per model, per token.** Every span is tagged with `sagewai.project_id`; per-tenant rollups drop out of the dashboard automatically.
- **Watch missions live.** The mission-control HUD streams agents, mission state, fleet posture, and live cost-per-run.
- **Read finance-friendly dashboards.** The Grafana board ships with five rows and 14 panels: system health, HTTP performance, status codes, OTel pipeline health, structured logs.
- **Audit business events.** Structured events (`setup.completed`, `auth.login.*`, `agent.created`, `agent.run.*`, `provider.test.*`) land in the log store and are queryable from the audit view.
- **Export to your SIEM.** The OTel pipeline emits OTLP; point an external collector at the same backend you use for the rest of your services.

## See it in action

### Primary example — live cost dashboard

[**Observability and cost**](/docs/tutorials/observability-and-cost) — Example 43 fires a mixed-tenant workload at the admin backend (real HTTP traffic, real OTel spans), and within three minutes both dashboards show real numbers. The page renders the dashboards from a real run, not pre-canned screenshots.

### Sibling examples

- [**Production multitenancy**](/docs/tutorials/production-multitenancy) — the per-tenant tagging that makes per-project cost rollups possible.
- [**Train your own model**](/docs/tutorials/train-your-own-model) — once the cost line is on the dashboard, the cost-down target is concrete.

### Pattern examples

- [**Example 34 — `observatory_cost_tracking`**](https://github.com/sagewai/platform/blob/main/packages/sdk/sagewai/examples/34_observatory_cost_tracking.py) — per-tenant cost tracking, the CFO line.
- [**Example 40 — `fleet_under_load`**](https://github.com/sagewai/platform/blob/main/packages/sdk/sagewai/examples/40_fleet_under_load.py) — 20+ workers, live HUD state.
- [**Example 12 — `budget_enforcement`**](https://github.com/sagewai/platform/blob/main/packages/sdk/sagewai/examples/12_budget_enforcement.py) — per-user/team/project budget caps.

## Where to go to ship it

- **[Observatory overview](/docs/platform/observatory)** — the HUD and the Grafana board, in detail.
- **[Admin Panel guide](/docs/guides/admin-panel)** — the surface where Audit, Runs, Profiles, and dashboards live.
- **[Cost management guide](/docs/guides/cost-management)** — strategies for keeping the bill down.

## See also

- **[Fleet](/docs/platform/fleet)** — the per-worker telemetry that Observatory aggregates.
- **[Security overview](/docs/platform/security)** — the redaction layer that makes telemetry safe to export.
- **[All products](/docs/platform)** — the other four.
